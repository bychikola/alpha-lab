# Alpha Lab — полигон для исследования крипторынков

**Дата:** 2026-09-11
**Статус:** дизайн утверждён, готов к планированию реализации
**Рабочая папка:** `C:\Users\Admin\Documents\Alpha MR`
**Озеро данных:** `D:\alpha-lab\data`

---

## 1. Контекст

В папке проекта лежит `index.html` (2551 строка) — лендинг для индикатора «Mean Reversion Quant Pro» на Pine Script v6. Внутри: описание модели (z-score, процесс Орнштейна–Уленбека, полужизнь, ATR-риск), два скрипта Pine, синтетическая демонстрация и Монте-Карло «бэктест».

**Ключевое ограничение текущего артефакта:** функция `runTrades()` (строка 2448) генерирует цену как `price = prev + θ(μ − prev) + σ·ε`, то есть проверяет стратегию на данных, порождённых той же моделью, которую стратегия эксплуатирует. Результат такого теста гарантирован математикой и ничего не говорит о рынке. Реальных рыночных данных в проекте нет.

**Цель проекта Alpha Lab:** построить исследовательский полигон, который позволяет проверять торговые гипотезы на реальных данных и — главное — **честно отбраковывать** те, что являются подгонкой под историю.

### Принятые решения

| Вопрос | Решение |
|---|---|
| Назначение | Реальная торговля своими деньгами |
| Рынок | Криптовалюты (Binance USDT-margined perpetual futures) |
| Горизонт | Движок агностичен: от минутных до дневных баров |
| Точка старта | Ядро + слой валидации (фазы 0–1) |
| Юниверс | Фокусированный: 20–30 ликвидных перпетуалов |
| Первый подопытный | Существующая MR-стратегия из `index.html`, перенесённая в Python |
| Визуализация | Дашборд нужен с фазы 0, не откладывается |

### Принцип, определяющий всю архитектуру

Полигон тратит большую часть усилий не на генерацию идей, а на различение закономерности и совпадения. В исторических данных можно подобрать что угодно; ценность представляет только умение отличить реальный edge от случайного. Поэтому слой валидации проектируется первым и делается «слепым» — он не знает о стратегии ничего, кроме её результатов.

---

## 2. Что НЕ входит в объём (YAGNI)

- **Глубокое обучение.** Машина: i5-1135G7, 4 ядра / 8 потоков, 16 ГБ RAM, без GPU. LSTM/трансформеры на тиках локально не поедут. Классическая статистика + градиентный бустинг — поедут.
- **HFT и торговля на микросекундах.** Требует колокации; у розницы латентность до API биржи 50–200 мс. Игра на этом поле проигрышна по построению.
- **Мульти-рынок (акции, форекс).** Только крипта. Абстракция под другие рынки не закладывается, пока нет конкретной межрыночной гипотезы.
- **Реальный ордер-роутинг в фазах 0–1.** Живое исполнение — фаза 4 и только для стратегий, переживших валидацию.
- **Веб-сервер и СУБД.** Дашборд — статический HTML, читающий данные. Никакого бэкенда. DuckDB используется как **встраиваемый** аналитический движок (библиотека, не сервер) — см. раздел 10.

---

## 3. Архитектура

### Принцип слоёв

Каждый слой знает только соседа снизу. Стратегия не знает, откуда данные. Валидатор не знает, что внутри стратегии.

```
D:\alpha-lab\
├── data\                          ← озеро данных (Parquet)
├── src\alpha_lab\
│   ├── data\        ingest · store · quality · universe
│   ├── features\    price · volatility · microstructure · labeling
│   ├── engine\      costs · broker · portfolio · backtest
│   ├── validation\  splits · metrics · significance · report
│   ├── strategies\  base · mean_reversion
│   ├── report\      schema · writer          ← JSON-контракт для дашборда
│   └── cli.py                                ← единая точка входа
├── tests\           синтетика · ловушки · инварианты
├── configs\         universe.yaml · experiments\*.yaml
├── notebooks\       черновики, не продакшн
└── dashboard\       index.html · app.js · style.css   (статический, без сборки)
```

### Границы модулей

| Модуль | Отвечает на вопрос | Зависит от |
|---|---|---|
| `data` | «Какие честные цены были?» | — |
| `features` | «Что можно посчитать по ценам?» | `data` |
| `engine` | «Что было бы, если бы я торговал?» | `features`, `engine.costs` |
| `validation` | «Это реально или мне показалось?» | `engine` (только результаты) |
| `strategies` | «Что делать?» | `features` |
| `report` | «Как это показать?» | `validation` (только Verdict) |

**Инвариант:** `validation` не импортирует `strategies`. Проверяется тестом. Это защита от «жалости к стратегии» — валидатор, который знает о стратегии больше, чем её трейды, можно случайно подкрутить.

---

## 4. Компоненты и интерфейсы

### 4.1 Strategy

```python
class Strategy(Protocol):
    name: str

    def generate(self, bars: pd.DataFrame) -> pd.Series:
        """Целевая позиция: -1.0 (полный шорт) … 0.0 … +1.0 (полный лонг).

        bars: колонки ts, open, high, low, close, volume, sorted by ts.
        Обязан использовать только информацию до момента t включительно.
        Проверяется автотестом look-ahead (см. 8.1).
        """
```

Интерфейс намеренно узкий: одна функция. Всё, что сложнее, — это уже композиция стратегий, а не новая абстракция.

### 4.2 CostModel

Итоговый интерфейс (план, Task 8; `engine/costs.py`):

```python
class CostModel(Protocol):
    def fee_bps(self) -> float: ...
    def slippage_bps(self, order_notional: float, bar_quote_volume: float) -> float: ...
    def funding_cost(self, position: float, rate: float) -> float: ...
```

Первоначальная редакция описывала интерфейс с `fee(symbol, ts, notional, maker)`, `slippage(bar, order_size)`, `funding(symbol, ts, notional)` и `borrow(...)` — он был заменён при планировании: ставки funding берутся из исторического ряда, выровненного по барам (`data/query.align_funding_to_bars`), а не запрашиваются у модели; `borrow` для USDT-перпетуалов не моделируется (его роль играет funding).

`slippage_bps` принимает размер заявки и котируемый объём **того бара, на котором исполняется заявка**, и является нелинейной функцией их отношения. Это принципиально: модель «проскальзывание = 1 тик» не различает $100 и $100k и делает невидимой зависимость результата от капитала.

`funding_cost` начисляется на удерживаемую позицию в каждом баре, где есть ненулевая историческая ставка (биржевой интервал — обычно 8 часов: 00:00, 08:00, 16:00 UTC). Без funding бэктест систематически завышает результат для всех стратегий, держащих позицию дольше нескольких часов. Если ряд ставок недоступен или пуст, CLI обязан предупредить, что издержки занижены (через `warnings` вердикта и отдельную строку в stderr), а не показывать funding = 0 как факт.

**Реализации:**
- `ZeroCost` — для инвариантных тестов.
- `RealisticCost` — тейкер/мейкер по фактическим тарифам Binance (VIP0), проскальзывание от доли объёма бара, реальные исторические ставки funding.

### 4.3 Validator

```python
@dataclass(frozen=True)
class Verdict:
    strategy_name: str
    sharpe: float
    dsr: float              # Deflated Sharpe Ratio
    pbo: float              # Probability of Backtest Overfitting
    p_value: float          # permutation-тест
    max_dd: float
    total_return: float
    trades: int
    n_configs_tried: int    # для поправки на множественные сравнения
    alive: bool
    reasons: tuple[str, ...]
    warnings: tuple[str, ...]  # гейты, НЕ проверенные в этом прогоне; на alive не влияют
    experiment_id: str      # хэш(конфиг + версия данных + git hash)
```

Вердикт бинарный — `alive: true/false` — плюс человекочитаемые причины. Формулировка «Sharpe 1.8, вроде неплохо» не допускается: она оставляет пространство для самообмана.

`reasons` и `warnings` — разные каналы. `reasons` объясняют, почему стратегия мертва; `warnings` фиксируют гейты, которые в данном прогоне не запускались (одиночная конфигурация без матрицы для PBO, отсутствие funding, разрывы данных). Непроверенный гейт не делает стратегию мёртвой — отсутствие входных данных не дефект стратегии, — но и не считается пройденным: отчёт, CLI и дашборд обязаны показать предупреждения явно.

---

## 5. Поток данных

```
Binance public data archive (data.binance.vision)
   │  monthly klines ZIP, бесплатно, без API-ключа, вся история
   ▼
[data.ingest]   →  Parquet, партиции symbol=/year=/month/, сжатие zstd
   │               оценка объёма: ~25 пар × ~6 лет ≈ 1.0–1.5 ГБ
   ▼
[data.quality]  →  гейт: пропуски, дубли, нулевые объёмы, аномальные спреды
   │               бары с флагом «грязный» исключаются из торговли
   ▼
DuckDB          ←  SQL прямо по Parquet, чтение только нужных колонок
   │               (16 ГБ RAM против десятков ГБ данных — обязательно)
   ▼
[features]      →  z-score, θ, полужизнь, ADF, Хёрст, ATR-режимы,
   │               funding, базис, объёмные метрики
   ▼
[strategy].generate()  →  целевые позиции (float, -1…+1)
   ▼
[engine]        →  с RealisticCost: комиссии + funding + проскальзывание + borrow
   ▼
trades + equity curve
   ▼
[validation]    →  Verdict (alive / dead)
   ▼
[report]        →  report.json + report.js  ──→  dashboard (статический HTML)
```

### Юниверс

Фокусированный набор 20–30 USDT-перпетуалов Binance, задаётся явным списком в `configs/universe.yaml`.

Правила формирования:
1. Только перпетуалы, торгующиеся не менее 2 лет (от даты листинга до сегодня).
2. Список **включает делистингованные пары** — иначе возникает ошибка выживаемости (стратегия тестируется только на выживших, убытки умерших не учитываются).
3. Список фиксирован на всё время исследования — изменение состава фиксируется в конфиге и меняет `experiment_id`.

Поле `include_delisted` (по умолчанию `true`) задаёт правило 2 явно. При `true` пара с оборванной историей остаётся в юниверсе, а ingest помечает её (`delisted`, `last_bar_ts`) и показывает счётчик делистингованных — молчаливого пропуска хвоста нет. При `false` такая пара исключается, но тоже явно (`excluded` + счётчик), с предупреждением, что это осознанный отказ от защиты и он создаёт ошибку выживаемости.

Период: с даты листинга каждой пары по настоящее время (максимум ~6 лет).

---

## 6. Слой валидации

Четыре механизма, каждый закрывает конкретный способ обмануть себя.

### 6.1 Purged K-Fold с embargo

**Проблема:** признаки считаются на скользящем окне (например, 20 баров). Train и test пересекаются по времени, и модель видит будущее через перекрытие окон. `train_test_split` этого не обнаруживает.

**Решение:** purge — из train удаляются все наблюдения, чьё окно признаков заглядывает в test; embargo — добавляется зазор после test-блока (признаки инерционны).

Реализация: `validation/splits.py`, параметры `n_splits`, `purge_window`, `embargo`.

### 6.2 Deflated Sharpe Ratio (DSR)

**Проблема:** при переборе N конфигураций выбранный по максимуму Sharpe почти наверняка завышен. Ожидаемый максимум Sharpe у N случайных стратегий растёт как `√(2·ln N)`; при N=500 это ≈ 3.5.

**Решение:** поправка Бейли и Лопеса де Прадо. Сырой Sharpe дефлируется на число испытаний, длину ряда, асимметрию и эксцесс распределения доходностей.

`n_configs_tried` берётся из журнала экспериментов, а не задаётся вручную.

### 6.3 Probability of Backtest Overfitting (PBO)

**Проблема:** «повезло на истории».

**Решение:** разбиение истории на блоки; проверка, как часто конфигурация, лучшая на одной половине, оказывается в худшей половине на другой. Высокое PBO означает подгонку.

### 6.4 Permutation-тест

**Проблема:** «а может, оно само так вышло».

**Решение:** перемешивание **позиций** относительно доходностей цены; пересчёт доходности стратегии на каждой перемешанной версии; построение распределения Sharpe под нулевой гипотезой; честный p-value.

Нулевая гипотеза формулируется как «сигнал не связан с доходностями». Перемешивать сами доходности нельзя: Sharpe = mean/std инвариантен к перестановке, поэтому такой тест дал бы p-value ≡ 1.0 и не отклонял бы ничего. Ломать нужно именно соответствие «сигнал ↔ доходность».

### 6.5 Пороги вердикта

Стратегия признаётся `alive` только при одновременном выполнении:
- `dsr > 0.95` — фактический порог кода (`min_dsr` в `validator.py`; первоначальная редакция говорила `dsr > 0`). DSR — это вероятность того, что истинный Sharpe больше нуля, и требование 0.95 означает 95%-ю уверенность, а не просто положительный знак оценки: порог намеренно консервативен и отсекает края, у которых «плюс» держится на нескольких наблюдениях.
- `pbo < 0.5` — **гейт фазы 2**. PBO считается по матрице доходностей многих конфигураций (T × N); одиночный прогон CLI такую матрицу не порождает. Когда матрицы нет, PBO не вычисляется, условие остаётся непроверенным, и вердикт сообщает об этом через канал `warnings` (не через `reasons`: отсутствие матрицы — свойство прогона, а не дефект стратегии). Когда матрица передана, но непригодна для CSCV (меньше 2 конфигураций или меньше 2·n_blocks наблюдений), вердикт, как и раньше, мёртв с явной причиной.
- `p_value < 0.05`
- `trades >= 100` (иначе статистики недостаточно)

Пороги задаются в конфиге, значения по умолчанию фиксируются в коде. При `trades < 100` вердикт — `alive: false` с причиной «недостаточно сделок», а не «не проверено».

---

## 7. Дашборд

**Форма:** статический HTML + vanilla JS + Canvas. Без сборки, без Node, без сервера. Открывается двойным кликом.

**Загрузка данных — без `fetch`.** Браузеры блокируют `fetch()` к локальным файлам по CORS при открытии через `file://`, поэтому чтение `report.json` напрямую невозможно. Движок пишет **два** файла с одним и тем же содержимым:
- `report.json` — машиночитаемый, для программ и тестов;
- `report.js` — тот же payload в виде `window.ALPHA_REPORT = { ... };`

Дашборд подключает `report.js` через `<script src="report.js">`, что работает на `file://` без сервера. Подход повторяет философию существующего `index.html` — самодостаточный файл, который просто открывается.

**Визуальный язык:** переиспользуется эстетика `index.html` — тёмный фон `#0b0e14`, акценты amber/sky/emerald, шрифты Inter + JetBrains Mono, Tailwind через CDN. Это не самоцель: единый вид проекта и лендинга.

**Содержимое (фаза 0–1):**
1. **Панель вердикта** — `alive`/`dead`, DSR, PBO, p-value, Sharpe, max DD, число сделок. Главный элемент экрана.
2. **Кривая доходности** — equity + drawdown под ней.
3. **Цена с сигналами** — бары, входы/выходы, полосы z-score.
4. **Распределение сделок** — гистограмма R-мультипликаторов.
5. **Панель издержек** — сколько съели комиссии, funding, проскальзывание по отдельности.

Пара `report.json` + `report.js` — **контракт** между движком и UI. Оба генерируются из одного объекта, поэтому разойтись не могут. Схема фиксируется в `report/schema.py` и версионируется полем `schema_version`; дашборд обязан отклонять неизвестную мажорную версию с внятным сообщением, а не молча показывать пустые графики.

---

## 8. Защита от самообмана

Это не обработка исключений, а встроенные гейты против когнитивных ошибок.

### 8.1 Почему look-ahead нельзя поймать статистикой

Первоначальная редакция этого раздела требовала, чтобы валидатор «убивал» стратегию, торгующую по будущему бару. **Требование невыполнимо, и это доказано экспериментально** (Task 17): подглядывающая стратегия получает `alive=True` с DSR 1.0 и p-value 0.001.

Причина фундаментальна. Валидатор видит только реализованную выборку — `returns`, `price_returns`, `positions`. Две гипотезы:

- **(A)** позиция `h_t` посчитана по информации до `t` и коррелирует с доходностью `r_t`;
- **(B)** позиция `h_t` посчитана из самого `r_t`.

...порождают **одинаковое совместное распределение** `(r_t, h_t)`. Никакая статистика по этой выборке их не различит: различие лежит в отображении «вход → позиция», а не в распределении выхода. Более того, любое дополнительное допущение (например, об эффективности рынка), которое позволило бы их различить, отвергало бы и настоящий edge.

Ни один из четырёх механизмов не помогает: purged CV не участвует в вердикте и не помог бы (утечка внутри строки); DSR и PBO измеряют смещение отбора по числу попыток; permutation-тест корректно разрушает связь «сигнал ↔ доходность» — а у подглядывающей стратегии позиции выровнены с доходностью **по построению**, и тест не может отличить это от мастерства.

**Следствие:** диагностика ёмкости (`over_capacity`) тоже не является детектором — замерено, что при капитале 10 она поднята и вердикт всё равно `alive=True`, а при капитале 1 опущена, и стратегия «зарабатывает» 1.79e9.

**Единственная работающая защита — запрос к функции решения.** `generate(bars[:k])` обязана побитово совпадать с `generate(bars)[:k]` для набора значений `k`. Это применяется к каждой стратегии через обязательный harness (`src/alpha_lab/causality.py`), а не как разовый тест одной модели. С фазы 1.5 harness вызывается и рабочим путём CLI: `_cmd_validate` проверяет стратегию **до** бэктеста и отказывает в вердикте, если проверка не пройдена (отключается только явным флагом `--skip-causality`, который громко предупреждает о цене).

**Точность гарантии.** Точка усечения `k` вскрывает зависимость от будущего ровно в одной позиции — последней в префиксе (`k−1`): все предыдущие позиции присутствуют и в усечённом прогоне, и в полном, поэтому утечка в них при данном `k` невидима. Отсюда политика плотности: при `n ≤ 600` точки берутся сплошь (полное покрытие всех позиций), выше — плотная сетка не реже 200 точек. **Утечка, целиком лежащая между точками разреза, на длинном ряде теоретически может остаться незамеченной** — для полной гарантии harness принимает `cut_points="all"`.

**Что остаётся незащищённым:** утечка на уровне признаков (признак, заглядывающий в `t+1` внутри окна нормализации) — harness судит по решению стратегии, а не по её входам; поведение последнего бара полного ряда (позиция `n−1` в точки усечения не входит); стратегия, ведущая себя причинно только под усечением. Первое закрывается причинностным тестом самих трансформов признаков. Отсутствие проверки у новой стратегии закрыто рабочим путём: `_cmd_validate` прогоняет harness до бэктеста, поэтому стратегия без причинности не получает вердикта; на реальном прогоне (35 064 бара) плотная сетка стоит ~1.6 с.

**Индексная конвенция (обязательна для стратегий).** `generate` обязан вернуть `Series`, чей индекс совпадает с `bars.index`. Движок `run_backtest` потребляет позиции позиционно и проверяет только длину, поэтому стратегия с дефолтным `RangeIndex` в нём работать будет, а harness её отвергнет. Это сделано намеренно: молчаливое выравнивание по меткам скрыло бы перестановку порядка.

| Ловушка | Механизм защиты |
|---|---|
| **Look-ahead** | **Ловится проверкой причинности над стратегией, а не статистикой над результатами.** Обязательный harness (`src/alpha_lab/causality.py`): `generate(bars[:k])` должна совпадать с `generate(bars)[:k]` для набора `k`. Применяется к **каждой** стратегии, а не только к MR, и в рабочем пути CLI: `_cmd_validate` вызывает harness до бэктеста, поэтому непрошедшая стратегия не получает вердикта. Статистический валидатор здесь бессилен по построению (см. 8.1). |
| **Выживаемость** | Делистингованные пары остаются в юниверсе; ingest помечает оборванную историю (`delisted`, `last_bar_ts`) и считает такие пары — пропуск хвоста не молчит. |
| **Грязные данные** | Бары с аномалиями/нулевым объёмом → стратегия не торгует (`clean_mask`). Интерполяция запрещена. Разрыв (пропущенные бары) не интерполируется и не делает прогон недействительным: CLI режет ряд на непрерывные участки и вызывает `generate` на каждом отдельно, поэтому история стратегии перезапускается на дыре по построению. Дополнительно `_gap_mask` обнуляет цель последнего бара перед дырой (`lookback=1`), и это несущая часть: движок удерживает `held[i] = target[i-1]`, так что без обнуления позиция прошла бы сквозь неизвестное движение в дыре и заработала бы `close[i]/close[i-1] − 1`. Вперёд маски нет (`lookahead=0`): решения нового участка опираются только на его контигуозные бары, а самая ранняя доходность его позиции — `close[i+1]/close[i] − 1`. Торговля останавливается ровно на баре перед дырой и возобновляется по мере прогрева стратегии на новом участке; `gaps`/`missing_bars`/`gap_masked_bars` видны в `extra`, предупреждение — в `warnings` вердикта. |
| **Множественные сравнения** | Каждый прогон логируется; DSR автоматически применяет поправку по числу конфигов в журнале. |
| **Невоспроизводимость** | `experiment_id = hash(config + data_version + git_hash)`. Один ID → один результат. |
| **Тихая деградация** | Любой отброшенный бар, пропущенная пара или упавшая проверка логируется в отчёт, а не проглатывается. |

---

## 9. Тестирование

Три уровня, каждый ловит свой класс ошибок.

### 9.1 Синтетика с известным ответом

OU-симуляция из `index.html` используется как фикстур. Истинные θ и σ известны, следовательно известно, должна ли MR-стратегия заработать и сколько именно.

**Критерий:** если полигон на этих данных говорит «мертва» — баг в движке, а не в рынке.

### 9.2 Ловушки

Фактический набор (`tests/fixtures/traps.py` и локальные классы `test_traps.py`):
- `LookAheadStrategy` — торгует по цене следующего бара.
- `PerfectForesightStrategy` — знает весь будущий ряд.
- `OverfitNoiseStrategy` — подогнана под конкретный исторический отрезок.
- `AlwaysLongStrategy` — всегда в лонге; контроль на отсутствие альфы (причинна, harness её пропускает).
- Локальные утечки в `test_traps.py` (`_SegmentLeak`, `_WarmupLeak`, `_ParityLeak`, `_MisalignedIndexStrategy`) — проверяют плотность сетки усечений и индексную конвенцию harness, а не валидатор.

`DirtyDataStrategy` из первоначальной редакции **не построена**: отдельной стратегии-ловушки нет. Грязные бары закрывает `clean_mask` (стратегия на них не торгует) плюс предупреждение CLI; см. критерий 3 в разделе 12.

**Критерий:** статистические ловушки (шум, подгонка под отрезок, недостаток сделок, множество попыток) обязан убить `validate()`; подглядывание (`LookAheadStrategy`, `PerfectForesightStrategy`) статистикой не ловится в принципе (8.1) — его обязан поймать причинностный harness `src/alpha_lab/causality.py`, вызываемый и тестами, и рабочим путём CLI до бэктеста. Это тест валидатора и harness, а не стратегий.

### 9.3 Инварианты издержек

- Нулевые комиссии → результат совпадает с наивным бэктестом.
- Комиссия 10% → стратегия разоряется.
- Рост издержек → монотонное падение доходности.

---

## 10. Технические решения

| Решение | Обоснование |
|---|---|
| **Python 3.14.6, управление через `uv` 0.12.3** | Уже установлены. Проверено `pip --dry-run`: колёса pandas, scipy, statsmodels, scikit-learn, pyarrow, duckdb, ccxt, arch, numba, lightgbm, polars резолвятся. Системный Python не трогаем; при проблемах `uv` откатит на 3.12 за секунды. |
| **pandas везде, Polars не тянем** | Тяжёлое чтение Parquet снимает DuckDB; в памяти остаются объёмы, где Polars не даёт выигрыша. `statsmodels`/`scipy`/`numpy` работают с pandas нативно. Два API датафреймов в одном проекте — лишняя сложность и лишние баги. |
| **DuckDB поверх Parquet** | 16 ГБ RAM против десятков ГБ данных. Плюс воспроизводимость: SQL-запрос сохраняется в конфиг эксперимента. |
| **numba для симуляции выходов** | Нахождение входов векторизовано (numpy), но SL/TP — путь-зависимы и требуют цикла. На 25 парах × 6 лет чистом Python это неподъёмно. При недоступности numba на 3.14 — `uv python pin 3.12`. |
| **Parquet + zstd на `D:\alpha-lab\data`** | На `C:` свободно 6 ГБ — ОС задохнётся. На `D:` — 50 ГБ. |
| **Дашборд без сборки** | Соответствует философии существующего `index.html`: открыл файл — работает. |
| **pytest** | Стандарт. |

**Структура зависимостей:** `uv` + `pyproject.toml`, группа `dev` для тестов.

---

## 11. Фазы

| Фаза | Содержание | Результат |
|---|---|---|
| **0. Срез** | Данные → признаки → бэктест → валидация → дашборд. Один сквозной прогон. | Работающий конвейер; цифры сходятся с TradingView на общем периоде. |
| **1. Ядро** | Полный слой валидации (DSR, PBO, purged CV, permutation); модель издержек с funding; дашборд целиком. | Полигон умеет **убивать** стратегии. |
| **2. Масштаб** | Свипы параметров, мульти-горизонт (1м…1д), батч-прогон гипотез, журнал экспериментов. | Тысячи конфигураций за ночь. |
| **3. Модели** | Коинтеграция и пары, LightGBM на признаках, RL для размера позиции. | Кандидаты в альфу. |
| **4. Live** | Paper-trading через ccxt, риск-менеджмент, реальное исполнение. | Реальные деньги — только для выживших. |

**Жёсткое правило:** фаза 4 наступает только для стратегий, переживших фазу 1. Не наоборот.

### Первый подопытный

MR-стратегия из `index.html` переносится в Python на фазе 0 как `strategies/mean_reversion.py`. Ожидание: вердикт может быть «мертва» — это допустимый и полезный результат, а не провал.

---

## 12. Критерии успеха фаз 0–1

1. `uv run alpha-lab ingest --config configs/universe.yaml` скачивает и раскладывает данные в `D:\alpha-lab\data`.
2. `uv run alpha-lab validate --config configs/experiments/mr_base.yaml` выдаёт `Verdict` и `report.json`. (Стратегия берётся из конфига; отдельного флага `--strategy` у CLI нет.)
3. Ловушки из 9.2 обезврежены (автотесты зелёные). Статистические (шум, подгонка под отрезок, нехватка сделок, множество попыток) убивает `validate()` — `test_all_traps_are_killed_by_validator`. Подглядывание (`LookAheadStrategy`, `PerfectForesightStrategy`) статистикой не ловится по построению (8.1); его ловит причинностный harness (`test_causality_harness_catches_*`), а в рабочем пути CLI стратегия, не прошедшая harness, не получает вердикта и отчёта (`test_non_causal_strategy_blocks_verdict_before_backtest`, exit code 2). Исходная формулировка «валидатор убивает все ловушки» неверна; `DirtyDataStrategy` из 9.2 не построена.
4. Контроль края: настоящий `MeanReversionStrategy` на синтетических `ou_bars` проходит рабочий путь `run_backtest` → `validate`. `test_mr_survives_validation_on_mean_reverting_ou` — theta=0.02 (полураспад 34.7 баров, то есть ДЛИННЕЕ окна z-скора 20; край снимает горизонт удержания max_bars=500 с геометрией 2×ATR стоп / 6×ATR тейк, а не окно входа — форвардная доходность лонга от z ≤ −2: +1.32% за 35 баров, t=8.6, и +3.19% за 500 баров, t=14.7, при издержках круга ≈11 bps) даёт `alive=True` (замер: 293 сделки, годовой Sharpe 1.96, DSR 0.9985, p 0.0010). `test_mr_is_dead_on_ou_without_reversion` — theta=0.002 (полураспад ≈ 347 баров; форвардная доходность на горизонте удержания +0.01%, t=0.1, издержки круга ≈11 bps её съедают) даёт `alive=False` за статистику, а не за нехватку сделок (368 сделок, DSR 0.636, p 0.217). Замер: «низкая theta» не синоним «нет края» — theta=0.01 (полураспад ≈ 69 баров) выживает на 8 из 10 seed'ов. Дополнительно сохранены generic-edge контроли: `test_strong_edge_survives_validation` — настоящий край у границы (годовой Sharpe ≈ 1.7 при пороге ≈ 1.1) остаётся `alive=True`; `test_edge_below_boundary_is_killed` — край слабее порога даёт `alive=False` за статистику, а не за отсутствие входов.
5. Дашборд открывается двойным кликом и отображает equity, просадку, вердикт и разбивку издержек.
6. Получен первый вердикт по реальной MR-стратегии — любой, включая «мертва».
7. Воспроизводимость: при идентичных входах (конфиг, символ), идентичной версии данных и журнале в одном и том же состоянии вердикт воспроизводится — статус (`alive`), все метрики вердикта, `reasons` и `warnings`, а также все ряды отчёта совпадают. Различается только `generated_at` — метка времени, которая по построению у каждого прогона своя. Побайтовое равенство `report.json` из исходной формулировки невыполнимо и гарантией не является: `n_trials` берётся из журнала попыток и растёт с каждым прогоном того же конфига, поэтому DSR (а у границы и `alive`) меняется от состояния журнала, а не от недетерминизма (замер: два прогона с общим журналом дали n_trials 1→2 и DSR 0.286→0.140 при том же `experiment_id`). Проверяется тестом `test_same_journal_state_gives_same_verdict`: два прогона CLI с чистым журналом, отчёты равны после удаления `generated_at`.

---

## 13. Риски

| Риск | Митигация |
|---|---|
| Пакет не собирается на Python 3.14 | `uv python pin 3.12` в проекте; системный Python не затрагивается. |
| Данные Binance archive недоступны из региона | Резервный источник — `ccxt` (REST, постранично). Скорость ниже, но работоспособно. |
| Диск `D:` заполнится | Оценка ~1.5 ГБ для фокусированного юниверса. Мониторинг размера в `ingest`. |
| Соблазн пропустить валидацию | Критерий 3 (ловушки) — обязательный тест, блокирующий. |
| Первая же стратегия окажется «мёртвой» и это демотивирует | Ожидание зафиксировано в разделе 11. Отрицательный результат — это результат. |

---

## 14. Открытые вопросы

Нет. Все решения, необходимые для планирования фаз 0–1, приняты.
